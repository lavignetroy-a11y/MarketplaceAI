# Approved reference images

**These are the design source of truth** — above both documents in `docs/source/`. Where a
document and a reference image disagree, the image wins.

## This folder is currently empty, and that's a problem

The approved images were shared inline in chat. They exist nowhere on disk, so they're lost to
anyone picking this project up — including a fresh AI session after context is summarised.

**Save them here.** Suggested names:

- `hero.png` — the hero section (the most-referenced one by far)
- `benefits.png` — "A better listing works before you answer a single message"
- `campaign-reveal.png` — "Not one edited photo. The full visual story."
- `how-it-works.png` — "From camera roll to ready to post."
- `trust.png` — "Better presented, still the same item."
- `categories.png` — "Made for what you sell."
- `examples.png` — dark proof section
- `pricing.png` — pricing grid
- `final-cta.png` — dark closing section

These are design references, not site assets — nothing renders them, so they don't need
compressing the way `public/images/` does.
